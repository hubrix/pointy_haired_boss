#+TITLE: Pointy Haired Boss — project tracker
#+AUTHOR: Mark Friedgan
#+TODO: TODO(t) NEXT(n) DOING(i) WAITING(w@) | DONE(d!) CANCELED(c@)
#+STARTUP: overview
#+OPTIONS: toc:2
#+LAST_UPDATED: [2026-09-19 Sat]

* Project charter
:PROPERTIES:
:CUSTOM_ID: charter
:END:

This file is the planning bible: the source of truth for work, priorities,
decisions, dependencies, and next steps. Agents must follow [[file:CLAUDE.md][the working rules]].
The specifications below support this tracker; they do not replace its live plan.
User instructions take precedence over recorded plans.

Build a prose-quality plugin for Codex, Claude Code, and DeepSeek Harness.
Use [[https://github.com/DietrichGebert/ponytail][Ponytail]] as the behavior reference. Improve readability, enforce house
style, remove unwanted text artifacts, and preserve the author's meaning.

Supporting documents:
- [[file:docs/RESEARCH.md][Research and implementation comparison]]
- [[file:docs/REQUIREMENTS.md][Product requirements]]
- [[file:docs/RULES.md][Rule catalog and examples]]
- [[file:docs/EVALUATION.md][Evaluation and release criteria]]
- [[file:docs/BASELINE.md][First-release implementation baseline]]
- [[file:docs/PARSER-SPIKE.md][Parser decision and measured evidence]]
- [[file:docs/HOST-SPIKE.md][Host contracts and partial smoke-test evidence]]
- [[file:docs/CONTRACTS.md][Implemented shared contracts and API behavior]]
- [[file:docs/CHECKER.md][Local checker usage, coverage, and known limits]]
- [[file:docs/UNICODE.md][Unicode inventory, cleanup policy, and application guarantees]]
- [[file:docs/STYLE.md][Readability measurement and the sourced Chicago review subset]]

Status vocabulary: TODO = queued; NEXT = ready; DOING = in progress;
WAITING = blocked with a recorded cause; DONE = complete with evidence;
CANCELED = abandoned with a recorded reason.

Tasks use stable CUSTOM_ID values. DEPENDS_ON lists prerequisite task IDs;
agents must check them because Org does not enforce this property. Completion
conditions describe required outcomes, not outcomes already achieved.

* Current focus and next actions
:PROPERTIES:
:CUSTOM_ID: focus
:END:

Stage: local checks, Unicode cleanup, reading-grade measurement, and a narrow
Chicago review subset implemented; host investigation remains partial.
Current checkpoint: PHB-023 complete. All 175 tests pass on Node 26.9.0 and 22.23.2.
The prior DSH probe passed eight service/event checks. Full host sessions remain untested.
Blockers: none. Contextual editing and installable host plugins remain unimplemented.
Chicago families retain partial coverage. Cleanup application is verified on macOS.

Next actions, in order:
1. [[#PHB-012][Complete live host invocation and lifecycle smoke tests]].
2. [[#PHB-024][Build contextual review and the bounded editing workflow]].
3. [[#PHB-025][Extend reviewed patches after the editorial workflow exists]].

Do not begin implementation merely because it appears here. Continue within
the user's current request and preserve later work in this file.

* Decisions and constraints
:PROPERTIES:
:CUSTOM_ID: decisions
:END:

** Confirmed by Mark

- [2026-09-19 Sat] Support Codex, Claude, and DeepSeek Harness. The working
  requirements interpret Claude as Claude Code.
- [2026-09-19 Sat] Use DietrichGebert/ponytail as the reference project.
- [2026-09-19 Sat] Target the DSH setup in
  =/Users/mark/code/scratch/homebrew-qwen-dsh=.
- [2026-09-19 Sat] Ban adverbs, passive voice, “this, not that” framing, and
  agentless obligations in the default house style.
- [2026-09-19 Sat] Prefer eighth-grade readability and follow Chicago for
  general style. Explicit house rules override that general guidance.
- [2026-09-19 Sat] Keep =project.org= as the canonical planning bible. Put
  update instructions in =CLAUDE.md= and capture future plans here.
- [2026-09-19 Sat] Push the current state and continue the project work.
- [2026-09-19 Sat] Continue implementation after the parser/host checkpoint.

** Working implementation baseline

[[file:docs/BASELINE.md][The first-release baseline]] adopts these reversible defaults under Mark's
instruction to continue. They are agent implementation choices, except for the
user-confirmed constraints above. [[file:docs/PARSER-SPIKE.md][The parser decision]] now has runnable evidence;
[[file:docs/HOST-SPIKE.md][host integration evidence]] remains partial.

| Topic | Working choice | Follow-up |
|-------+------------------+-----------|
| Name and command | Pointy Haired Boss; pointy-haired-boss; phb | PHB-010 |
| First release | English text/Markdown; general nonfiction, business prose, docs | PHB-010 |
| Activation | Normal prose guidance while enabled; manual-only option and off switch | PHB-010 |
| Punctuation | Review overuse; no global em-dash ban unless requested | PHB-010 |
| Style reference | Chicago 18th edition; publish supported subset | PHB-023 |
| Readability metric | Flesch–Kincaid target at most 8.0; warn rather than lose meaning | PHB-023 |
| Core | Node, remark/unified with GFM/front matter/math, compromise candidates | PHB-011, PHB-020 |
| Host scope | All three adapters in the first release | PHB-012 |
| Voice samples | Preserve source voice; sample-derived profiles in a later increment | PHB-040 |
| Watermark work | Unicode cleanup in MVP; statistical work in a separate experiment | PHB-050 |

[[file:docs/CONTRACTS.md][Version 1 contracts]] fix configuration precedence, matching boundaries,
severity/evidence semantics, source offsets, and coverage reporting. The house
profile now includes nine configurable phrase bans and the technical-term allowlist.
The checker defaults to exact phrase, boundary, and Unicode inventory coverage;
all four contextual house bans remain visible as partial candidate checks. Explicit
required checks can demand broader coverage. [[file:docs/STYLE.md][READ-01]] now reports a pinned
English metric with a 100-word minimum and explicit unavailable states; it does
not certify comprehension. Six sourced Chicago candidates retain partial family
coverage, with the consulted edition visible for each rule. [[file:docs/UNICODE.md][Cleanup]] removes an initial BOM run
by default, with a preserve option; interior artifact deletion requires an explicit
ASCII-word policy. These are agent implementation choices under the request to
continue, documented with the [[file:docs/CHECKER.md][checker behavior]].

The DSH wrapper pinned =0.1.6-alpha.1= and Cordis =4.0.2= at initial inspection.
Recheck those pins during the host spike. A separate local DSH checkout and the
global =dsh= binary used older versions; neither establishes wrapper compatibility.

* Milestone 0: discovery and planning foundation

** DONE Research existing approaches
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-001
:END:
Evidence: [[file:docs/RESEARCH.md][research report]] compares Ponytail, prose skills, linters, readability
guidance, Unicode cleaners, watermark research, and host integration surfaces.
Verification: reviewed primary documentation/source; no independent tool-quality
or performance benchmarks. Later research must keep those limits explicit.

** DONE Draft requirements and record the confirmed house style
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-002
:END:
Evidence: [[file:docs/REQUIREMENTS.md][requirements]] and [[file:docs/RULES.md][rules]] include all four bans, eighth-grade
readability, Chicago guidance, fidelity checks, and adapter requirements.
Verification: checked document links and the JSON configuration example.
Scope: drafting is complete; open product choices and implementation remain open.

** DONE Define evaluation and release criteria
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-003
:END:
Evidence: [[file:docs/EVALUATION.md][evaluation plan]] defines fixtures, baselines, metrics, and end-to-end
scenarios. Verification: reviewed coverage against the draft requirements.
No corpus, benchmark run, or passing release result exists yet.

** DONE Establish the canonical Org tracker and shared agent instructions
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-004
:END:
Completion: seed completed and remaining work; link all specifications; require
tracker updates in =CLAUDE.md=; make Codex/DSH discover the same rules; validate
Org structure, task links, dependencies, and local document links.
Evidence: [[file:project.org][this tracker]], [[file:CLAUDE.md][shared working instructions]], and [[file:AGENTS.md][Codex/DSH entry point]].
Verification: Emacs Org lint passed; all 33 stable IDs are unique; links and
dependencies across 27 tasks resolve without cycles. README and requirements
point back to this tracker. No implementation task has started.

** DONE Commit and push the planning baseline
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-005
:END:
Initial state: no local commits or usable origin URL; no matching repository
under the authenticated hubrix account. Created private hubrix/pointy_haired_boss,
committed the planning files, and pushed main. Private visibility was an agent choice.
Completion: verify the remote main revision matches the local baseline commit.
Evidence: [[https://github.com/hubrix/pointy_haired_boss][private GitHub repository]], baseline commit
=208170cb34366f178986e080819643fa30314f38=. Local HEAD and remote main matched
after the push; main tracks origin/main. Repository visibility verified PRIVATE.

* Milestone 1: settle the baseline and prove the integration

** DONE [#A] Resolve the remaining first-release choices
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-010
:DEPENDS_ON: PHB-002
:END:
Outcome: a documented baseline for scope, activation, name, genres, punctuation,
voice samples, and watermark timing. Preserve Mark's confirmed defaults.
Use existing authorization and judgment for routine reversible choices; ask
only when a material product choice cannot be inferred.
Completion: record decisions above and align [[file:docs/REQUIREMENTS.md][requirements]] with them.
Evidence: [[file:docs/BASELINE.md][baseline decision table]] records scope, activation, naming,
punctuation, voice, and watermark timing; requirements point to the same baseline.
Verification: checked against the confirmed house rules and requested hosts.

** DONE [#A] Compare parser and checker options
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-011
:DEPENDS_ON: PHB-010
:END:
Compare textlint with a focused core built on a proven Markdown parser.
- [X] Check protected spans, Markdown structure, offsets, and patch generation.
- [X] Check grammar-analysis options for adverbs/passive voice and candidate reporting.
- [X] Measure dependency footprint, startup cost, and local execution behavior.
- [X] Record the chosen engine, tradeoffs, and supported input boundaries.
Completion: a small runnable spike and a decision with evidence; no framework
choice based on feature lists alone.
Evidence: [[file:docs/PARSER-SPIKE.md][decision report]], [[file:spikes/parser/compare.mjs][runnable comparison]], and
[[file:docs/spikes/parser-results.json][raw measurements]]. =npm test= passes 25 tests. Remark matches 16/16
parser-boundary fixtures; textlint matches 15/16. Both expose three policy gaps.
Compromise matches 16/20 grammar examples; it remains a candidate generator.
Follow-ups: PHB-020, PHB-021, and PHB-025 cover source mapping, boundary policy,
grammar gaps, and production patch behavior. This completes a spike, not the checker.

** NEXT [#A] Verify the three host integration surfaces
:PROPERTIES:
:CUSTOM_ID: PHB-012
:DEPENDS_ON: PHB-010
:END:
- [X] Record actual Codex and Claude Code versions and their plugin/hook contracts.
- [X] Verify the DSH wrapper's installed runtime pins and Cordis service APIs.
- [X] Compare default DSH discovery paths with the wrapper's seeded skill location.
- [ ] Verify effective skill discovery in the complete wrapper composition.
- [ ] Prove manual invocation and bounded policy activation on each host.
- [ ] Document hook trust, compaction/resume, off-switch, and enforcement limits.
Completion: recorded smoke-test evidence for all three hosts and an adapter
contract that does not assume identical hook schemas.
Requirements: HOST-01–03, ACT-01–03, DIST-01.
Partial evidence: [[file:docs/HOST-SPIKE.md][host contract report]] and [[file:docs/spikes/dsh-results.json][eight passing DSH checks]].
The native probe tests skill loading, scope isolation, disposal, custom roots,
and synthetic pre-step events. It does not prove live model or durable session behavior.
Next: create temporary adapter packages, verify Codex/Claude discovery and native
validation, then exercise invocation, off/re-enable, resume, and compaction on
all three hosts. Verify the wrapper's explicit route to any .gsd skill root;
prefer direct bundled registration for PHB. Do not treat the path gap as a proven
defect in the user's effective wrapper configuration.

* Milestone 2: build the first usable release

** DONE Define shared rule, profile, and diagnostic contracts
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-020
:DEPENDS_ON: PHB-011
:END:
Implement versioned rule records, severity, evidence types, allowlists,
suppressions, configuration precedence, input hashes, offsets, and coverage status.
Use the selected remark core. Specify decoded-to-source mapping, incomplete
boundary/semantic coverage, scalar columns, CRLF, and invalid UTF-8 handling.
Completion: validated schemas and focused fixtures for invalid configuration,
Unicode positions, exact phrase boundaries, and protected-content conflicts.
Requirements: RULE-01–03, VOICE-01, OUT-01, OUT-03.
Implementation: publish strict versioned JSON schemas, catalog records, profile/config
resolution, exact phrase and suppression contracts, original-source positions,
and diagnostic/coverage validation. Reject ambiguous or unsupported configuration.
Keep the first rule checks and full Markdown boundary policy under PHB-021.
Evidence: [[file:src/contracts/][contract modules]], [[file:schemas/][six generated JSON Schemas]],
[[file:rules/catalog.json][40 rule records]], [[file:profiles/house.json][house profile]], and [[file:docs/CONTRACTS.md][API contract]].
Verification: 38 contract tests plus the 25 parser-spike tests pass on Node 26.8.2
and 22.23.2; generated contracts match their definitions; documentation examples
validate. Coverage tests distinguish candidates, confirmed violations, suppression,
protected conflicts, and missing checks. Metadata does not imply detector availability.
Decoded Markdown source mapping and file/profile discovery remain in PHB-021;
patch application remains in PHB-025. No plugin or host lifecycle claim follows
from this task's completion.

** DONE Implement parsing, local checks, and the four house bans
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-021
:DEPENDS_ON: PHB-020
:END:
Protect code, quotations, links, citations, math, front matter, HTML, and locked
spans. Enforce phrase bans; detect adverbs, passive voice, contrastive framing,
and agentless obligations. Separate candidates from contextual confirmations.
Spike follow-ups: inline quotations, author-date citations, defined lock syntax,
inline HTML, unknown AST nodes, sentences crossing formatting, negation adverbs,
passive infinitives, and reduced passives. Keep meaning conflicts unresolved
instead of deleting negation or inventing an actor.
Implemented: read-only =phb check= for stdin, files, directories, and selections;
explicit configuration/profiles, nested Git ignore rules, include/exclude filters,
decoded source mapping, protected spans, trusted lock/suppression comments, nine
phrase defaults, required terminology/allowlists, and all four house-ban candidates.
JSON/text reports retain source positions, hashes, suppressed findings, skipped
inputs, and every omitted rule. Default required coverage covers boundaries and
LEX-01; requiring unfinished/contextual checks returns exit 2. No automatic edits.
Completion: =check= works on supported inputs with correct exclusions, findings,
coverage, and exit behavior; fixtures include grammar lookalikes and missing owners.
Requirements: IN-01–02, RULE-04, OUT-03, SAFE-01.
Evidence: [[file:src/check/][checker modules]], [[file:bin/phb.mjs][CLI entry point]], [[file:test/check/][40 checker tests]], and
[[file:docs/CHECKER.md][usage and limits]]. All 103 tests pass on Node 26.9.0 and 22.23.2.
The production parser preserves all 16 structural spike fixtures; candidate checks
match all 20 small labeled grammar examples, including the four prior misses.
This is fixture coverage, not real-world precision/recall or semantic confirmation.
Known limits: raw HTML protects its enclosing block; unusual quote/citation forms
need a lock; incomplete boundaries return 2. Global Git excludes and automatic
user/profile discovery remain unsupported. PHB-022 extends default coverage with
Unicode inventory; contextual enforcement remains PHB-024; host packaging stays separate.

** DONE Implement Unicode inspection and conservative cleanup
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-022
:DEPENDS_ON: PHB-020 PHB-021
:END:
Inventory original code points, explain each finding, and implement =clean=
without stylistic paraphrasing. Preserve legitimate script/emoji/bidi/space uses.
Implemented: UNI-01–06 inventory on original source, named character/context
reports, preserve/review/remove decisions, cleanup previews, and explicit file
application. The checker now requires enabled Unicode inventory checks by default.
Bundled data and license remove runtime download requirements. Schemas cover
inventory and cleanup plans; source/output hashes bind every deletion to its input.
Implementation choice: pin Unicode/Emoji 17.0 data and license. Default cleanup
removes an initial UTF-8 BOM run; =unicode.bom= can preserve it. Explicit
=unicode.remove= / =--remove= can authorize U+200B, U+00AD, U+2060, or interior
U+FEFF inside ASCII words. No other character removal or global normalization.
Inventory legitimate characters without classifying their presence as a violation.
Cleanup-only application regenerates the plan, copies native file metadata,
rechecks identity/bytes, and performs a per-file atomic replacement. General
editorial patches remain PHB-025. Document the final check-to-rename race and
per-file batch behavior; do not claim a filesystem compare-and-swap guarantee.
Completion: cleanup is idempotent, reports exact edits, and preserves the Unicode
counterexamples and protected spans. Ambiguous artifacts remain findings.
Requirements: UNI-01–03, PRIV-01, IN-02.
Evidence: [[file:src/unicode/][Unicode modules]], [[file:docs/UNICODE.md][policy and guarantees]], [[file:data/unicode-manifest.json][pinned data manifest]],
[[file:test/unicode/][37 new tests]], and [[file:schemas/cleanup.schema.json][cleanup plan schema]]. All 140 tests pass on
Node 26.9.0 and 22.23.2. Fixtures retain legitimate scripts, emoji, selectors,
accents, spacing, bidi, tags, and protected markup. Tests cover idempotence, stale
and forged plans, modes, macOS ACLs/extended attributes, per-file failures, visible
controls, and execution with Node networking APIs disabled.
Limits: no confusables audit, universal sequence validation, or statistical-watermark
test. Linux native-copy behavior and other OS writers remain release follow-ups.

** DONE Implement eighth-grade readability and a checked Chicago subset
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-023
:DEPENDS_ON: PHB-020 PHB-021
:END:
- [X] Define segmentation, syllable estimation, exclusions, and short-sample behavior.
- [X] Implement Flesch–Kincaid measurement and the grade-eight target warning.
- [X] Refine CMO-01–06 into sourced, testable rules with edition and exceptions.
- [X] Preserve citation facts and document house-style precedence.
Implementation choices: parse-english 7.0.0 sentence/word units and syllable 5.0.1
estimates; whole eligible body sentences only; report original ranges, counts,
exclusions, and unavailable grades. Undefined number/initialism pronunciations
do not gain fabricated syllable counts. Short samples retain an insufficient status.
Chicago screens em-dash spacing, colon capitalization, ebook spelling, month-first
ordinal days, Latin-abbreviation commas, and outside quotation punctuation.
All six families remain partial. Four checks cite explicit 18th-edition guidance;
two identify 17th-edition Q&As without claiming their 18th-edition text was checked.
Completion: metric fixtures and declared Chicago checks pass; unsupported guidance
stays visible and essential facts survive efforts to lower reading burden.
Requirements: READ-01–02, STYLE-01–02.
Evidence: [[file:src/style/][style modules]], [[file:docs/STYLE.md][counting policy and sourced subset]],
[[file:test/style/][35 new tests]], and [[file:schemas/readability.schema.json][metric]] / [[file:schemas/chicago.schema.json][Chicago]] schemas.
Verification: all 175 tests pass on Node 26.9.0 and 22.23.2; generated assets match.
A production-only install passes Node 22 metric/Chicago and Unicode-preview smoke
checks. Fixtures cover known counts, protected text, dates, quotations, selections,
partial suppressions, thresholds, and read-only CLI behavior. No prose is rewritten.
Limits: the metric implements requirement READ-02; contextual requirement READ-01
remains PHB-024. Wider Chicago/citation review remains PHB-024/031. Pronunciation
coverage expansion remains PHB-041; exact minimum-version/performance checks PHB-032.

** NEXT Implement the shared editorial skill and fidelity review
:PROPERTIES:
:CUSTOM_ID: PHB-024
:DEPENDS_ON: PHB-020 PHB-021
:END:
Provide =review=, =rewrite=, and =write= using the host-selected model. Keep the
short active policy separate from full reference catalogs. Preserve voice,
claims, modality, negation, attribution, and supported responsibility.
Use the sourced Chicago definitions and metric availability without treating
candidate detection or a grade as semantic confirmation. Review wider title,
compound, number, abbreviation, and citation conventions only with checked sources.
Completion: the bounded edit/verify loop handles clean prose, missing substance,
all four bans, and unfixable conflicts without fabricating facts or cycling.
Requirements: EDIT-01–04, VOICE-01, READ-01, SAFE-01, PRIV-01.

** TODO Implement reviewed patches and command behavior
:PROPERTIES:
:CUSTOM_ID: PHB-025
:DEPENDS_ON: PHB-021 PHB-022 PHB-024
:END:
Provide text, JSON, and diff outputs; handle existing edit authorization;
reject stale/overlapping fixes; apply per-file atomic writes; report batch failures.
Reuse the verified Unicode cleanup writer and plan-regeneration checks; expand
them for editorial patches and diffs without weakening source or protected boundaries.
Extend the source-patch spike with reversible decoded-text mappings, insertion
semantics, grapheme boundaries, and selected-range checks.
Define automatic user configuration/profile discovery for shared CLI/host use;
the first checker loads project .phb.json and explicit user/profile files only.
Completion: review stays read-only, failed validation preserves original content,
and file-edit fixtures pass. No extra approval round for an authorized edit.
Requirements: OUT-01–03, IN-01–02.

** TODO Package the Codex adapter
:PROPERTIES:
:CUSTOM_ID: PHB-026
:DEPENDS_ON: PHB-012 PHB-024 PHB-025
:END:
Completion: installable plugin with shared skills, lifecycle configuration,
on/off/status, intensity selection, and a manual fallback; versioned smoke-test
evidence covers trust/degraded mode, resume/compaction, and session isolation.
Requirements: ACT-01–03, HOST-01, HOST-03, DIST-01.

** TODO Package the Claude Code adapter
:PROPERTIES:
:CUSTOM_ID: PHB-027
:DEPENDS_ON: PHB-012 PHB-024 PHB-025
:END:
Completion: installable plugin with shared skills and Claude-specific lifecycle
handling; smoke tests cover activation, status, disabling, restoration, and
bounded checks after writes/stops.
Requirements: ACT-01–03, HOST-01, HOST-03, DIST-01.

** TODO Package the native DSH adapter
:PROPERTIES:
:CUSTOM_ID: PHB-028
:DEPENDS_ON: PHB-012 PHB-024 PHB-025
:END:
Completion: ESM/Cordis plugin works in the confirmed wrapper's runtime and local
model configuration, registers shared skills, restores policy state, and requires
no extra cloud key. Verify enable/disable and session isolation in the wrapper.
Requirements: ACT-01–03, HOST-02–03, PRIV-01, DIST-01.

* Milestone 3: verify and prepare release

** TODO Build the annotated evaluation corpus
:PROPERTIES:
:CUSTOM_ID: PHB-030
:DEPENDS_ON: PHB-003 PHB-010
:END:
Create the proposed 200-passage corpus with source/licensing metadata, development
and frozen test splits, facts, expected findings, and unchanged controls.
Completion: all slices in [[file:docs/EVALUATION.md][the evaluation plan]] have coverage and near-duplicate
or author-sample leakage checks. Use fixtures during implementation, not just at release.

** TODO Run cross-model editing and preservation evaluations
:PROPERTIES:
:CUSTOM_ID: PHB-031
:DEPENDS_ON: PHB-023 PHB-026 PHB-027 PHB-028 PHB-030
:END:
Compare baselines and our tooling on recorded host/model versions. Measure
house-ban compliance, fidelity, reader preference, clean-prose preservation,
readability, and false positives. Include independent human review as specified.
Completion: a report with sample counts, actual results, failures, and limits;
open a tracker task for every unresolved release issue.

** TODO Verify lifecycle, privacy, patch safety, and performance
:PROPERTIES:
:CUSTOM_ID: PHB-032
:DEPENDS_ON: PHB-026 PHB-027 PHB-028
:END:
Run the end-to-end acceptance scenarios and local-network-disabled checks.
Measure checker runtime and policy overhead on a documented reference machine.
Test the declared Node 22.18 minimum and pinned host versions. The 175-test checker,
style, Unicode, contract, and parser suite passes on Node 26.9.0 and 22.23.2; the exact 22.18 minimum
remains unverified. Measure rich Markdown, large inputs/protected-range sets, and
the complete rule pipeline;
the repeated-paragraph parser timings do not establish the PERF-01 release gate.
Validate Linux native metadata copying, filesystem flags/permission failures,
and failure-path temporary-file cleanup. The macOS writer preserves ACLs and
extended attributes in tests; changed-file application on other OSes is unavailable.
The networking-API-disabled CLI tests cover current local commands, not future hosts.
Completion: record measured results against [[file:docs/EVALUATION.md][release gates]], with no false claims
about universal enforcement, watermark removal, or untested host versions.
Requirements: ACT-01–03, OUT-01–03, PRIV-01, SAFE-01, PERF-01.

** TODO Prepare release artifacts and installation documentation
:PROPERTIES:
:CUSTOM_ID: PHB-033
:DEPENDS_ON: PHB-031 PHB-032
:END:
Pin dependencies; check actual licenses and attribution; validate manifests;
document install, usage, configuration, capabilities, update, and uninstall.
Completion: a reviewable release candidate with linked verification results.
Publishing or changing external installations follows the user's authorization
at that time; this task does not itself authorize those actions.
Requirements: DIST-01, HOST-01–03.

* Backlog: later capabilities
:PROPERTIES:
:CUSTOM_ID: backlog
:END:

These items preserve future plans. Their order and release assignment remain
proposals. Add newly discovered follow-ups here or to the appropriate milestone.

** TODO Add sample-derived voice profiles
:PROPERTIES:
:CUSTOM_ID: PHB-040
:DEPENDS_ON: PHB-024
:END:
Outcome: reviewable profiles from user-provided samples with explicit storage
scope, privacy behavior, and evidence of voice retention. Requirement: VOICE-02.

** TODO Expand genres, languages, and document formats
:PROPERTIES:
:CUSTOM_ID: PHB-041
:END:
Evaluate one extension at a time: domain-specific prose, non-English editorial
rules, HTML/MDX, or rich documents. Require language/format-specific preservation
fixtures before claiming support. Scope each candidate here before implementation.
For English metrics, define sourced number/initialism pronunciations or explicit
user syllable overrides before expanding availability. Test ambiguous numbers,
years, abbreviations, all-caps prose, names, and dialect variation; never remove
meaning-bearing tokens merely to obtain a grade.

** TODO Evaluate interoperability and CI integrations
:PROPERTIES:
:CUSTOM_ID: PHB-042
:DEPENDS_ON: PHB-020 PHB-025
:END:
Consider Vale/textlint rule integration, SARIF/pre-commit/CI reporting, and MCP
only where a real workflow needs them. Record scope, costs, and acceptance criteria
before promoting any option into the active plan.

** TODO Evaluate a Claude.ai/Desktop skill package
:PROPERTIES:
:CUSTOM_ID: PHB-043
:DEPENDS_ON: PHB-024
:END:
Outcome: a separate support decision and packaging plan; do not confuse Claude
Code plugin support with every Claude product surface.

* Experimental track: statistical watermarks
:PROPERTIES:
:CUSTOM_ID: watermark-track
:END:

Unicode cleanup does not prove statistical-watermark removal. Keep the requested
statistical topic visible even if the first release does not include it.

** DONE Decide whether statistical mitigation is a launch requirement
CLOSED: [2026-09-19 Sat]
:PROPERTIES:
:CUSTOM_ID: PHB-050
:DEPENDS_ON: PHB-010
:END:
Current proposal: a separate experimental milestone. Completion: record its
scope and timing decision; update the release plan if it becomes launch-critical.
Decision: retain the separate experiment as the working baseline; no universal
removal claim in the MVP. Evidence: [[file:docs/BASELINE.md][baseline]] and [[file:docs/REQUIREMENTS.md][WM-01–06]].
This is a reversible implementation choice under the continuation request;
PHB-051 tracks the actual experiment and remains open.

** TODO Build a controlled, named-scheme watermark experiment
:PROPERTIES:
:CUSTOM_ID: PHB-051
:DEPENDS_ON: PHB-050 PHB-024
:END:
Use marked/unmarked samples, compatible tokenizer/configuration, named detectors,
calibration, and held-out verification. Compare untouched text, Unicode cleanup,
ordinary editing, and bounded experimental rewrites.
Completion: reproducible results for the named configuration, including fidelity,
voice, uncertainty, latency, and failure outcomes. Meet WM-01–06 before claiming
a verified capability; preserve the original on failed quality checks.

** TODO Assess file and media provenance as a separate extension
:PROPERTIES:
:CUSTOM_ID: PHB-052
:END:
Track possible PDF/DOCX metadata, C2PA/EXIF, and media processing work without
folding it into the prose MVP. Completion: a scoped proposal or a cancellation
decision with rationale; a text rewrite cannot establish container cleanup.

* Progress log
:PROPERTIES:
:CUSTOM_ID: progress-log
:END:

** [2026-09-19 Sat] Research and requirements baseline

Completed PHB-001 through PHB-003. Recorded Mark's four bans, eighth-grade
preference, Chicago guidance, Ponytail reference, and the DSH wrapper path.
Checked relative document links and the illustrative JSON configuration.
Implementation, evaluation data collection, and runtime benchmarks remain open.

** [2026-09-19 Sat] Canonical tracker established

Completed PHB-004. Seeded 27 tasks spanning completed research, product choices,
implementation, release verification, later capabilities, and watermark experiments.
Added CLAUDE.md update rules and the AGENTS.md entry point. Emacs Org lint and
the checks for IDs, local links, and dependency cycles passed. PHB-010 is the next
ready task; all implementation work remains queued.

** [2026-09-19 Sat] Publish the baseline and continue

Mark requested a push and continued work. PHB-005 is active. The destination
needs setup because origin has no URL; use a private repository under the
authenticated account. Continue with baseline choices and parser/host spikes.

Baseline push completed as commit =208170c= to private origin/main. PHB-010 is
active; use the existing proposals as reversible implementation defaults, keeping
the confirmed house rules and the watermark topic in scope.

Completed PHB-010 and PHB-050 with the working baseline in docs/BASELINE.md.
Started PHB-011 and PHB-012. Detected Node 26.8.2, Codex CLI 0.154.0, and Claude
Code 2.1.278; the exact DSH runtime still needs its own probe. Parser code and
measurements are development spikes, not a finished checker or plugin.

** [2026-09-19 Sat] Parser decision and native DSH contract probe

Completed PHB-011: 25 tests pass, source patches reject stale/overlapping/protected
edits, and a recorded comparison selects remark/unified. Compromise is a candidate
generator with four misses in 20 grammar examples. Protected quotation/citation/lock
policies, decoded source mapping, and expanded grammar coverage remain required.

PHB-012 has partial evidence: eight checks pass against the wrapper's installed
DSH 0.1.6-alpha.1 and Cordis 4.0.2. The probe covers native service APIs and synthetic
events. Full invocation, effective wrapper roots, durable mode state, resume,
compaction, and pre-delivery enforcement remain open. No user host settings changed.

PHB-020 is the next core task. PHB-012 returns to NEXT with concrete live-test steps.
All follow-ups appear in their owning tasks. This checkpoint includes the working
baseline, spike code, lockfile, measured reports, and updated requirements/README.
Verification: clean =npm ci --include=dev --ignore-scripts= succeeded; all 25
parser/patch tests passed. Emacs Org lint passed. The 34 IDs and 28 tasks have
valid links and acyclic dependencies; local document links and JSON examples
passed validation. The checkpoint is ready for commit and push to origin/main.

** [2026-09-19 Sat] Shared contracts implemented

Completed PHB-020 after Mark asked to keep working. Added strict versioned schemas,
all 40 catalog records, a house profile, traced configuration/profile precedence,
literal Unicode phrase matching, source hashes/positions, suppression validation,
fix preconditions, and coverage/exit semantics. All four bans persist across intensity
modes. Explicit style settings can override them; fidelity safeguards remain active.

All 63 tests pass on Node 26.8.2 and Node 22.23.2. Generated contracts match their
definitions, and both documented configuration examples pass the runtime resolver.
Recorded complete API behavior in docs/CONTRACTS.md. The pipeline still needs
file discovery, Markdown boundaries, actual rule execution, and host adapters.
PHB-021 is NEXT; PHB-012 retains its live-test steps. No user host settings changed.
Emacs Org lint, local links, JSON examples, and the checks for 34 unique IDs,
28 task dependencies, and dependency cycles all pass.

** [2026-09-19 Sat] First runnable local checker

Completed PHB-021 after Mark asked to keep going. Added read-only checks for stdin,
files, directories, and selections; configuration/profile loading; nested ignore
handling; text/JSON reporting; original-source mappings; quotation/citation/HTML
protection; trusted inline directives; vocabulary/terminology checks; and candidates
for the four house bans. The nine-phrase house pack remains configurable.

All 103 tests pass on Node 26.9.0 and 22.23.2, including 40 new checker tests.
Regression fixtures cover the 16 structural parser examples and all 20 small
grammar examples. Fixed continuation indentation and selection-created word
boundaries during verification. Generated contracts match their definitions.
Documented that local success covers requested checks only, with every semantic,
Unicode, readability, and Chicago omission visible. No source files or host
settings change during checks. PHB-022 is NEXT; the host spike retains its remaining
live-test steps. Wider corpus, release performance, and minimum-version testing
remain open.
Final checks: production-only dependency installation and the Node 22 CLI smoke
test pass. Emacs Org lint, all 34 IDs/28 task dependencies, local document links,
JSON examples, and =git diff --check= pass. This checkpoint is ready for commit
and push to origin/main.

** [2026-09-19 Sat] Unicode inventory and conservative cleanup

Completed PHB-022 after Mark asked to keep going. Added Unicode/Emoji 17.0 data,
checksums and license; inventory for all six Unicode families; original names,
positions and visible contexts; conservative BOM handling; explicit ASCII-word
artifact policies; and =clean= preview/application. No script, emoji, bidi, space,
accent, or tag stripping occurs by default. Inventory does not claim AI authorship
or statistical-watermark removal.

Added two schemas and 37 tests; all 140 tests pass on Node 26.9.0 and 22.23.2.
Cleanup rescans output for idempotence, preserves protected text, and revalidates
current bytes/policy before writing. Atomic replacement now preserves native
metadata; macOS tests cover permissions, extended attributes, and ACLs. Runtime
commands also pass with Node networking APIs disabled. Source position lookup
now caches scalar counts to avoid rescanning long lines for each inventory item.

PHB-023 is NEXT, with concrete metric/source-checking steps; host lifecycle work
remains PHB-012. Linux writer validation, filesystem edge failures, the exact Node
minimum, wider corpus, and full release measurements remain in PHB-032. General
editorial patches remain PHB-025. No user's prose or host settings changed while
developing this increment; application tests use temporary fixtures.

Final checks: a production-only dependency install passes the Node 22 cleanup
application and idempotence smoke test. Emacs Org lint, 34 unique IDs, 28 task
dependencies, local links, three runtime-validated configuration examples, and
=git diff --check= pass. This checkpoint is ready for commit and push to origin/main.

** [2026-09-19 Sat] Reading-grade measurement and sourced Chicago candidates

Completed PHB-023 after Mark asked to keep going. Added body-prose metric units,
pinned sentence/word and syllable analysis, a grade-eight warning, explicit short
sample/unsupported-pronunciation states, and six narrow Chicago candidate checks.
Each Chicago rule records its consulted edition, source, scope, and exceptions.
All CMO families remain partial; exact quotations and citation fields stay intact.
The four house bans retain their defaults and contextual-review requirements.

Added two schemas and 35 tests. All 175 tests pass on Node 26.9.0 and 22.23.2.
Tests caught and now cover prototype-property words in the abbreviation lookup;
metric findings also avoid letting a single-word exemption suppress a document
warning. A production-only install passes Node 22 metric/Chicago and Unicode
preview smoke checks. The existing networking-API-disabled check/clean tests pass.

PHB-012 is next for live host invocation and lifecycle evidence. PHB-024 is ready
for contextual editing; wider Chicago judgment and citation consistency belong
there and in PHB-031 evaluation. Pronunciation expansion is recorded in PHB-041;
the exact Node minimum, wider corpus, and performance remain release checks.
No user prose or host settings changed during this increment.

Final checks: Emacs Org lint, 34 unique IDs, 28 task dependencies and cycle checks,
local document links, three runtime-validated configuration examples, and
=git diff --check= pass. This checkpoint is ready for commit and push to origin/main.
